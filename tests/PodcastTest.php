<?php

use Kiwilan\Rss\Feed;
use Kiwilan\Rss\Tests\Utils\XmlReader;

it('can make podcast feed', function () {
    $feed = Feed::make();

    // ray($render);
    // XmlReader::make($render)->save(__DIR__.'/feed.xml');
});
